/**
 * Configuración de la aplicación
 * Edita estos valores según tu entorno
 */
window.AppConfig = {
  // MercadoPago - Solo claves públicas (nunca pongas el ACCESS_TOKEN aquí)
  mercadoPago: {
    publicKey: "", // Clave pública de sandbox válida
    environment: "sandbox" // "sandbox" o "production"
  },
  
  // Configuración de la app
  environment: "production",
  debug: false
};